---
name: x402 premium AI gate
status: open
created: 2026-03-18T23:25:41Z
updated: 2026-03-18T23:25:41Z
github: (will be set on sync)
depends_on: ["003"]
parallel: true
conflicts_with: []
---

# Task 008: x402 premium AI gate

## Description

Gate the premium receipt parsing tier behind an x402 micropayment. Free tier: ≤ 5 line items. Premium: unlimited, multi-currency, handwritten receipts. Payment: 0.01 TON. Fully isolated — disabling `X402_ENABLED` leaves core unchanged.

## Acceptance Criteria
- [ ] `lib/x402/gate.ts` — x402 middleware for Next.js API routes
- [ ] `app/api/receipts/parse/route.ts` updated — checks premium flag + x402 gate
- [ ] `components/premium-gate.tsx` — UI shown when free tier limit hit
- [ ] Free tier: ≤ 5 items parsed, standard speed
- [ ] Premium tier: unlimited items, multi-currency, handwritten
- [ ] `X402_ENABLED=false` → gate is bypassed, core flow unchanged
- [ ] Demo-able: show paywall, show unlock, show premium parse

## Technical Details

### x402 middleware
```typescript
import { withPaymentRequired } from "x402-next"
export const POST = withPaymentRequired(handler, {
  amount: "0.01",
  currency: "TON",
  description: "SatSplit premium parse",
})
```

### Feature flag check
```typescript
if (!process.env.X402_ENABLED || process.env.X402_ENABLED === "false") {
  return handler(req) // bypass gate entirely
}
```

## Dependencies
- Task 003 (parse route exists)

## Effort Estimate
- Size: S
- Hours: 3

## Definition of Done
- [ ] Code implemented
- [ ] Paywall shown when free tier exceeded
- [ ] x402 payment unlocks premium parse
- [ ] Feature flag bypasses gate cleanly
